import 'package:flutter/Material.dart';

class AdminLateEmployeePage extends StatelessWidget {
  const AdminLateEmployeePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("Late Employee Page"),
      ),
    );
  }
}
