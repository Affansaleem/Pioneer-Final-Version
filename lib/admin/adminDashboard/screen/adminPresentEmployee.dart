import 'package:flutter/Material.dart';

class AdminPresentEmployeePage extends StatelessWidget {
  const AdminPresentEmployeePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("Present Employee Page"),
      ),
    );
  }
}
