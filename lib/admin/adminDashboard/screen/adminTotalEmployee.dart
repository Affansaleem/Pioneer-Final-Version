import 'package:flutter/Material.dart';

class AdminTotalEmployeePage extends StatelessWidget {
  const AdminTotalEmployeePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("Total Employee Page"),
      ),
    );
  }
}
