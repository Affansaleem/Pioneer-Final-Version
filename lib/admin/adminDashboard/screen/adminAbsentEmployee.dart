import 'package:flutter/Material.dart';

class AdminAbsentEmployeePage extends StatelessWidget {
  const AdminAbsentEmployeePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text("Absent Employee Page"),
      ),
    );
  }
}
